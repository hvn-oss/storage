/** The only protocol version supported by the initial v1 control boundary. */
export const storageProtocolVersion = "1";

/** Names the header that carries the HVN Storage protocol version. */
export const storageProtocolVersionHeader = "HVN-Storage-Version";
